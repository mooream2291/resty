// import React from './react';
// import Results from './results';
//if there is a props condition, return it, is not NULL//
const If = props => {
    return props.condition ? props.children : null;
  }
  
  export default If;